/** @type {import('tailwindcss').Config} */
module.exports = {
  prefix: 'vs-', // optional, only if you want to use a prefix
  content: ["./**/*.liquid"], // fixed glob
  theme: {
    extend: {},
  },
  plugins: [],
}
